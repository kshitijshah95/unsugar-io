# Production Environment - Configuration Only (No Secrets!)
# Used when running: npm run build
# 
# IMPORTANT: This file is tracked in git
# All values here are PUBLIC (exposed to browser)
# Set secrets in Netlify dashboard instead
#
# ===========================================

# Production Backend URL (PUBLIC - safe to track)
VITE_API_BASE_URL=https://unsugar-io-api.onrender.com

# Environment (PUBLIC)
VITE_NODE_ENV=production

# ===========================================
# Note: Frontend has NO secrets!
# All sensitive data lives in backend
# Vite exposes ALL VITE_* variables to browser
# ===========================================
