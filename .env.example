# Frontend Environment Variables Example
# Copy this file to create your environment-specific files:
# - .env.development (for local development)
# - .env.production (for production builds)
# - .env (optional, for local overrides)

# ===========================================
# Backend API URL
# ===========================================

# For local development (use local backend)
VITE_API_BASE_URL=http://localhost:3001

# For production (use deployed backend)
# VITE_API_BASE_URL=https://unsugar-io-api.onrender.com

# ===========================================
# Environment Identifier
# ===========================================

VITE_NODE_ENV=development

# ===========================================
# How Vite Loads Environment Files:
# ===========================================
# 
# When you run:
#   npm run dev       → Loads .env.development (+ .env)
#   npm run build     → Loads .env.production (+ .env)
#   npm run preview   → Loads .env.production (+ .env)
# 
# Priority (higher overrides lower):
#   1. .env.[mode]  (mode-specific)
#   2. .env         (shared for all modes)
# 
# IMPORTANT: Only variables prefixed with VITE_ are exposed to client code!
# ===========================================
